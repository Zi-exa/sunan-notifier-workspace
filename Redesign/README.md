
  # Redesign with Same Palette

  This is a code bundle for Redesign with Same Palette. The original project is available at https://www.figma.com/design/xRowbo7OMS9wnKAigRfcg7/Redesign-with-Same-Palette.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  